{
  "buildCommand": "pnpm drizzle-kit migrate && pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/[...path].ts": { "maxDuration": 10 }
  }
}
